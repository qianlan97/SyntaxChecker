Success: no syntax error is found.

Following is the indentation-updated source code:

func main::(a::int, b::int, a::int)->int
begin
    var y::int;
    var x::int;
    x <- x + 1;
    while (1 = 2)
    begin
        x <- x(1, 2, 3, 4, (5 + x + y * z)) + (2 + 3) * (4 + 5 * 6);
    end
end

